# 🏠 บ้านเราซื้ออะไร — เอกสารออกแบบ

หลักคิดเดียว: **ซื้อของซ้ำ ไม่ควรต้องพิมพ์ซ้ำ** → เปิด → แตะ → ปรับจำนวน → ส่ง
ผู้ใช้จริงสั่งสัปดาห์ละครั้ง จึงออกแบบให้ "สั่งเหมือนครั้งที่แล้ว" เป็นทางลัดหลัก

---

## 1. UX Flow

```
เข้าใช้ครั้งแรก:  /join (รหัสบ้าน + PIN) → /who (ใครใช้เครื่องนี้) → หน้าสั่งของ
ครั้งต่อไป:       เปิดแอป → หน้าสั่งของทันที (cookie จำไว้ 1 ปี)
```

**Flow 1 — แตะเลือก (ไม่กี่วินาที)**
เปิดแอป → เห็น ⭐ ซื้อประจำ → แตะของ (ได้จำนวนเริ่มต้นทันที) → กด +/− บนการ์ดถ้าต้องปรับ → แถบล่าง "🛒 ส่งรายการ" → ขึ้น "ส่งรายการแล้ว 💚"

**Flow 2 — สั่งเหมือนครั้งที่แล้ว**
กด 🔄 → ของรอบก่อนถูกติ๊กครบ (จำนวน/หมายเหตุตามรอบก่อน) → เอาออก/เพิ่ม/ปรับ → ส่ง

**Flow 3 — ของใหม่ (ทำครั้งเดียว)**
＋ เพิ่มของ → ชื่อ (พิมพ์) + แตะเลือกหน่วย/จำนวน → บันทึก → ของถูกติ๊กให้ทันที ครั้งหน้ามีให้แตะ

**Flow 4 — อีกคนไปซื้อ**
เปิดแท็บ 📋 ต้องซื้อ → แตะแถวเพื่อติ๊ก ✓ (ของที่ซื้อแล้วจางและลงไปอยู่ส่วน "ซื้อแล้ว") → ครบแล้วกด "เสร็จสิ้นรอบนี้"
อีกเครื่องเห็นการติ๊กแบบเรียลไทม์

**กฎที่ตัดสินใจไว้**
| เรื่อง | กฎ |
|---|---|
| รายการที่กำลังซื้อ | ครอบครัวมีได้ **ใบเดียว** ส่งซ้ำระหว่างนั้น = รวมเข้าใบเดิม (สินค้าซ้ำรวมจำนวน, หมายเหตุต่อกัน, ติ๊กซื้อแล้วถูกรีเซ็ต) |
| ⭐ ซื้อประจำ | สั่งแล้วตั้งแต่ 2 ครั้ง เรียงตาม use_count แล้วดู last_ordered_at ที่เหลือไปอยู่ "➕ ของอื่น ๆ" (พับได้) ช่วงแรกที่ยังไม่มีประวัติจะโชว์ทุกอย่างรวมกัน |
| use_count | +1 ต่อสินค้า ต่อการส่ง 1 ครั้ง (ไม่นับตามจำนวนชิ้น) |
| สั่งเหมือนครั้งที่แล้ว | ใช้ใบล่าสุดของ **ครอบครัว** รวมเข้ากับที่เลือกอยู่ (ไม่ทับ) ข้ามของที่เก็บไปแล้ว ไม่แก้ใบเก่า |
| ตะกร้าที่กำลังเลือก | เก็บในเครื่อง (localStorage) จนกว่าจะกดส่ง ปิดแอปแล้วไม่หาย |
| ลบสินค้า | ไม่ลบ แต่ "เก็บ" (archived_at) เพราะประวัติยังอ้างอิง |
| แก้รายการที่ส่งแล้ว | กดค้างที่แถวในหน้า "ต้องซื้อ" → แก้จำนวน/หมายเหตุ หรือเอาออก (เฉพาะใบที่กำลังซื้อ) · คนอื่นได้แจ้งเตือน Telegram · เอาชิ้นสุดท้ายออก = ลบใบว่างทิ้ง · ไม่ย้อน use_count (แก้ที่ส่งผิดไม่ทำให้ของขึ้น ⭐ เพราะต้องสั่งครบ 2 ครั้ง) |

---

## 2. Wireframe (มือถือ 375px)

**หน้าสั่งของ (`/`)**
```
┌──────────────────────────────┐
│ ☀️ เช้านี้                    │
│ สวัสดี อ้วน 👋            (👨)│
│ ┌──────────────────────────┐ │
│ │ 📋 มีของต้องซื้ออยู่ 5 จาก 8 ›│ │  ← แสดงเมื่อมีรายการค้าง
│ └──────────────────────────┘ │
│ 🛒 วันนี้ซื้ออะไรดี?  [＋ เพิ่มของ]│
│ แตะของที่ต้องการได้เลย         │
│ ┌──────────────────────────┐ │
│ │ 🔄 สั่งเหมือนครั้งที่แล้ว      │ │
│ │    รอบเมื่อวาน · 8 รายการ    │ │
│ └──────────────────────────┘ │
│ ⭐ ซื้อประจำ                   │
│ ┌────────────┐┌────────────┐ │
│ │     🥩     ││     🍗     │ │
│ │   หมูสันใน   ││   ปีกไก่    │ │
│ │  × 1 ชิ้น   ││ [−] 8 [+]  │ │ ← เลือกแล้ว: ขอบเขียว พื้นมิ้นต์
│ └────────────┘└────────────┘ │
│ ...                          │
│ ➕ ของอื่น ๆ (6)            ▾ │
│        กดค้างที่ของเพื่อแก้ไข     │
│        📖 ประวัติการสั่ง         │
├──────────────────────────────┤
│ ┌────┐ ┌──────────────────┐  │ ← แถบติดล่าง (เมื่อมีของที่เลือก)
│ │📋 8│ │   🛒 ส่งรายการ    │  │
│ └────┘ └──────────────────┘  │
├──────────────────────────────┤
│   🛒 สั่งของ     📋 ต้องซื้อ ③  │ ← แท็บล่าง
└──────────────────────────────┘
```

**Sheet ตรวจรายการ (แตะ 📋 8)** — ไว้ใส่หมายเหตุ / ลบ / ปรับจำนวน
```
┌──────────────────────────────┐
│ ▬▬                         ✕ │
│ 📋 รายการที่เลือก (8)          │
│ ┌──────────────────────────┐ │
│ │ 🍗 ปีกไก่     [−] 8 [+]    │ │
│ │ 📝 เพิ่มหมายเหตุ            │ │
│ └──────────────────────────┘ │
│ ...                          │
│ ล้างทั้งหมด  [ 🛒 ส่งรายการ ]   │
└──────────────────────────────┘
```

**หน้าต้องซื้อ (`/list`)**
```
📋 รายการที่ต้องซื้อ
👨 อ้วน  👩 หมวย สั่งไว้
▓▓▓▓░░░░░░  เหลือ 5 จาก 8
┌──────────────────────────────┐
│ ○  🦐 กุ้งแชบ๊วย        × 8 ตัว │  ← แถวสูง ≥64px แตะได้ทั้งแถว
│ ○  🎃 ฟักทอง           × 1 ลูก │
│      📝 เหลือง ๆ ส้ม ๆ         │
└──────────────────────────────┘
✅ ซื้อแล้ว (3)
│ ●  🥩 หมูสันใน  (จาง+ขีดฆ่า)   │
         [ ✅ เสร็จสิ้นรอบนี้ ]     ← ปุ่มใหญ่เมื่อครบ
```

**หน้าเพิ่ม/แก้ของ (sheet)**: ตัวอย่างรูป+ช่องชื่อ → แถบเลือก emoji (เดาให้จากชื่อ) → ชิปหน่วย → จำนวนเริ่มต้น [−][+] → หมายเหตุเริ่มต้น → [เพิ่มของ]

---

## 3. UI Design System

**สี** (พาสเทลใช้เป็นพื้นหลังเท่านั้น ตัวอักษรใช้สีเข้มเพื่อให้อ่านออกกลางแดด)
| ชื่อ | ค่า | ใช้กับ |
|---|---|---|
| cream | `#fbf9f2` | พื้นหลังหน้า |
| ink | `#213a2d` | ตัวอักษรหลัก (เขียวเข้มเกือบดำ) |
| ink-soft | `#586e60` | ตัวอักษรรอง (คอนทราสต์ ≥ 5:1 บนขาว) |
| mint 100/200 | `#e1f6e8` `#c5ecd3` | การ์ดที่เลือก, ขอบ, พื้นปุ่มรอง |
| mint 700 | `#257c50` | ปุ่มหลัก (ตัวขาวบนพื้นนี้ ≈ 5.3:1), ตัวเลขเน้น |
| air 100/200 | `#e0f0fa` `#c2e2f4` | ฟ้าอ่อนสำหรับ "สั่งเหมือนครั้งที่แล้ว" และแบนเนอร์ |
| rose | `#fdeceb` / `#a4413a` | ข้อความผิดพลาด/เตือน |

**ตัวอักษร** Noto Sans Thai 400/600/700 (โปร่ง อ่านง่าย ไม่เด็กเกินไป) — หัวเรื่อง 24/20/18, เนื้อหา 15–16, คำอธิบาย 11–14 ช่องกรอกใช้ 16px เพื่อไม่ให้ iOS ซูมเอง

**รูปทรง** การ์ดสินค้า 24px · ปุ่ม/แถว 16–24px · sheet มุมบน 28px · ปุ่มกลม (avatar, ชิป) เต็มวงกลม
**เงา** ไม่ใช้ ใช้ขอบ 2px สีมิ้นต์แทน (ตามสเปก: ไม่ใช้เงาหนัก/gradient)
**ขนาดกด** ปุ่มทุกตัว ≥ 44px (+/−, ✕, ชิป, แถวรายการ ≥ 64px)
**เลย์เอาต์** คอนเทนเนอร์กว้างสูงสุด 576px กึ่งกลาง · กริดสินค้า 2 คอลัมน์ (320–559px), 3 คอลัมน์ (≥560px) · ไม่มี horizontal scroll ทั้งหน้า (แถบ emoji เลื่อนแนวนอนอยู่ใน sheet เท่านั้น)
**Safe area** ใช้ `viewport-fit=cover` + `env(safe-area-inset-bottom)` กับแท็บล่างและแถบส่ง
**Motion** เฉพาะ 4 จุดที่สเปกกำหนด: เลือกสินค้า (pop), เปลี่ยนจำนวน (bump), ส่งรายการ (💚 pop + sheet เลื่อนขึ้น), ติ๊กซื้อแล้ว (pop) · เคารพ `prefers-reduced-motion`

---

## 4. Component Structure

```
app/
  layout.tsx · manifest.ts · globals.css
  join/page.tsx            เข้าบ้าน (รหัส + PIN)
  join/new/page.tsx        สร้างบ้านใหม่
  who/page.tsx             ใครใช้เครื่องนี้
  (app)/                   ต้องเข้าบ้านและเลือกสมาชิกแล้ว
    layout.tsx             แท็บล่าง + RealtimeSync + ตัวเลขของที่ค้าง
    page.tsx               หน้าสั่งของ → <OrderScreen/>
    list/page.tsx          ต้องซื้อ → <Checklist/>
    history/page.tsx       ประวัติ
    history/[id]/page.tsx  รายละเอียด + <ReorderAgain/>

components/
  OrderScreen   ศูนย์กลางหน้าสั่งของ: ตะกร้า(draft) กริด แถบส่ง toast หน้ายืนยัน
  ├─ ProductCard   การ์ดสินค้า: แตะ=เลือก, +/−, กดค้าง=แก้ไข
  ├─ ReviewSheet   ตรวจรายการ/หมายเหตุ/ลบ/ส่ง
  ├─ ProductSheet  เพิ่ม/แก้/เก็บสินค้า (ใช้ suggestEmoji, UNITS)
  └─ Sheet         bottom sheet พื้นฐาน
  Checklist     ติ๊กซื้อแล้ว (useOptimistic) + เสร็จสิ้นรอบ + กดค้างเพื่อแก้
  └─ ItemSheet    แก้จำนวน/หมายเหตุ/เอาออก (ใช้ useLongPress)
  Tabs          แท็บล่าง 2 แท็บ + ตัวเลขค้าง
  RealtimeSync  รับสัญญาณ broadcast → router.refresh()
  ReorderAgain  ใส่ของรายการเก่ากลับเข้าตะกร้า
  JoinForm · CreateForm · AvatarPick
  useDraft      ตะกร้าใน localStorage

lib/
  actions/auth.ts   createFamily · joinFamily · selectMember · leaveFamily
  actions/shop.ts   submitOrder · toggleItem · completeList · saveProduct · archiveProduct
  queries.ts        getProducts · getActive · getLastList · getHistory · getListDetail · getMembers
  session.ts (cookie เซ็น HMAC) · pin.ts (scrypt) · auth.ts (requireMember)
  realtime.ts (ส่งสัญญาณ) · db.ts (service role) · emoji.ts · units.ts · format.ts · types.ts
supabase/migrations/001_init.sql · 002_telegram.sql · 003_item_edit.sql
```

**ข้อมูลไหลอย่างไร**
`Server Component` ดึงข้อมูล → ส่งเป็น props ให้ Client Component → แตะ/กด → `Server Action` (ตรวจ cookie ทุกครั้ง) → เขียน DB → `revalidatePath` + ส่งสัญญาณ Realtime → เครื่องอื่น `router.refresh()`

---

## 5. Database Schema

```
families ──< family_members
   │
   ├──< products
   │
   └──< shopping_lists ──< shopping_list_items >── products (nullable, on delete set null)
        (active ได้ใบเดียว/ครอบครัว)
```

| ตาราง | คอลัมน์สำคัญ |
|---|---|
| families | id, name, **code** (unique), pin_hash (scrypt), **realtime_key** (สุ่ม) |
| family_members | id, family_id, name, avatar, sort_order, **user_id** (เผื่อ Auth จริง) |
| products | id, family_id, name, **emoji**, unit, default_quantity, default_note, **use_count**, **last_ordered_at**, archived_at |
| shopping_lists | id, family_id, created_by, status `active｜done`, created_at, completed_at |
| shopping_list_items | id, list_id, product_id, **name_snapshot**, emoji, quantity, unit, note, is_purchased, purchased_at, added_by |

- **ไม่มี `shopping_history`** — ประวัติคือ `shopping_lists` + items (มี snapshot อยู่แล้ว)
- `remove_list_item()` (003) เอาของออกแบบล็อกแถวใบ active เหมือน `submit_order()` ไม่ชนกับอีกเครื่องที่กดส่งพอดี
- `unique index one_active_list_per_family` บังคับให้มีรายการ "กำลังซื้อ" ได้ใบเดียว
- `submit_order()` (SQL function) ทำทั้งหมดในทรานแซกชันเดียว: หา/สร้างใบ active → เพิ่ม/รวมรายการ → เก็บ snapshot → use_count+1 → ป้องกันสองเครื่องส่งพร้อมกัน
- **ความปลอดภัย:** เปิด RLS ทุกตารางและไม่มี policy → anon key อ่าน/เขียนไม่ได้เลย เข้าถึงผ่าน Server Actions (service role) ที่ตรวจ cookie เท่านั้น
- **ต่อยอด Auth จริง:** ผูก `family_members.user_id` กับ `auth.users` แล้วค่อยเพิ่ม RLS ตาม family_id — โครงสร้างตารางไม่ต้องเปลี่ยน
- **แจ้งเตือน Telegram (ทำแล้ว):** `family_members.telegram_chat_id` (migration 002) · ผู้รับ = สมาชิกที่เชื่อมแล้ว **ยกเว้นคนสั่ง** · ผูกบัญชีผ่านลิงก์ `t.me/<bot>?start=<code เซ็นลายเซ็น>` + webhook `/api/telegram` (ตรวจ secret header) · ส่งด้วย `after()` หลังตอบผู้ใช้แล้ว ล้มเหลวไม่กระทบการสั่ง · ผู้ใช้บล็อกบอท (403) → ล้าง chat_id อัตโนมัติ
- **กันโปรเจกต์ฟรีถูกพัก + โซน:** `vercel.json` ตั้ง region `sin1` และ cron เรียก `/api/keepalive` วันละครั้ง (ตรวจ `CRON_SECRET`)
- **ต่อยอด LINE/Web Push:** เพิ่มช่องทางที่จุดเดียวกับ `notifyBuyers()` ใน `lib/telegram.ts`

---

## บันทึกการตัดสินใจ (ต่างจากสเปกตรงไหน)
1. การ์ดสินค้า **2 คอลัมน์** บนมือถือ เพื่อให้ปุ่ม +/− ได้ 44px บนการ์ด
2. **แถบส่งรายการติดล่าง** แทนปุ่มท้ายหน้า + ตัดส่วน "รายการที่เลือก" ยาว ๆ เหลือ sheet ตรวจรายการ
3. รวม "รายการล่าสุด" เข้ากับปุ่ม 🔄 (ทำหน้าที่เดียวกัน) รอบเก่ากว่านั้นดูใน ประวัติ
4. Realtime ใช้ **Broadcast เป็นสัญญาณ** ไม่ต้องทำ Supabase Auth ใน MVP
5. เพิ่ม `products.emoji`, `archived_at`, "➕ ของอื่น ๆ" (กันของใหม่หาย)
